using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingDamage //: MonoBehaviour
{
    /*
    private ObjectPooler _objectPooler;

    private void Start()
    {
        _objectPooler = ObjectPooler.Instance;
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.gameObject.layer == LayerMask.NameToLayer("ChasingBullet"))
        {
            SpawnDamageParticle(this.transform.position);
        }
        SpawnDamageParticle(this.transform.position);
    }

    public void SpawnDamageParticle(Vector2 position)
    {
        _objectPooler.SpawnFromPool("DamageParticle", position, new Quaternion(Random.Range(0f, 1f), Random.Range(0f, 1f), Random.Range(0f, 1f), Random.Range(0f, 1f)));
    }
    */
}

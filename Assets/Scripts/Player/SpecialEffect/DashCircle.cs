using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DashCircle //: MonoBehaviour
{
    /*
    private PlayerBehaviour m_PlayerBehaviour;

    private void Start()
    {
        m_PlayerBehaviour = FindObjectOfType<PlayerBehaviour>();
    }

    private void FixedUpdate()
    {
        transform.position = new Vector2(transform.position.x - (Time.deltaTime * m_PlayerBehaviour.m_PlayerPhysicsBehaviour.m_Direction), transform.position.y);
    }
    */
}
